Daily Toolkit — Critical Issues Fix Pack (2026-07-31)
======================================================

37 files, same folder structure as your repo (Ahsan098-2/aum123).
Upload these to GitHub replacing the existing files at the same paths.
tools.html is NEW — add it at the repo root.

Recommended upload order:
1. sitemap.xml
2. tools.html (new)
3. index.html
4. Everything else in tools/ and the root pages (about, blog, contact, privacy, terms)

What changed in each file:
- sitemap.xml: 34 .html entries -> clean URLs (www host), added /tools
- All 35 existing pages: canonical tag fixed (www host, no .html), internal
  links de-.html'd, index.html links -> "/", apex domain -> www everywhere
- tools/bmi-calculator.html: fixed "Pension_calculator.html" typo/casing
- tools/instagram-caption-maker.html: fixed 3 wrong-level sibling links,
  removed the broken Word Finder link
- tools/pdf-merger.html: fixed 4 wrong-level sibling links
- tools.html: NEW /tools hub page (reuses your existing nav/footer/design,
  full searchable + filterable tool grid, categories section)

After uploading, re-crawl with Ahrefs and I can verify the live site again.
